services:
  frontend-mock:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    networks:
      - alphapulse-network
    container_name: alphapulse-frontend-mock
    # depends_on: [fastapi]  # If you want to wait for fastapi, uncomment and ensure fastapi is defined elsewhere

networks:
  alphapulse-network:
    external: true
    name: alphapulse-network
